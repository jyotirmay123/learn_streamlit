# 7Learnings ML Models and shared code

- install dependencies to develop package
`pip install -e .[tests]`

- run all tests
`pytest`

- run single test
`pytest common/tests/test_sales_prophet.py::TestModel::test_train_history`
